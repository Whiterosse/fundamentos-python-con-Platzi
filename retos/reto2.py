"""🛠️ Reto 2: El Generador de Reportes de Ventas (Nivel: Intermedio)
*   **Problema:** Necesitas registrar de forma persistente las ventas del día en un archivo de texto llamado `ventas_diarias.txt`.

*   **Tu Misión:**
    1. Usa `with open` en modo escritura (`"w"`) para crear el archivo y escribir tres líneas, cada una con el formato:
    `Plataforma - Precio` (ej. `Netflix - 15000`).
    2. Luego, usa `with open` en modo lectura (`"r"`) para abrir ese mismo archivo, leer su contenido línea por línea y
    mostrarlo en la terminal con un formato elegante.
"""
